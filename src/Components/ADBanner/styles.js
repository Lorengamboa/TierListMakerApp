import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  bottom: {
    position: "absolute",
    bottom: 0
  },
  top: {
    position: "absolute",
    top: 0
  }
});

export default styles;
